1. Download the dataset:
   - Books.csv
   - Ratings.csv
   - Users.csv

2. Load the CSV data into the database (if using SQLite, MySQL, or another DBMS).

3. Use the `INSERT INTO` statements from `sampleData.sql` to populate the tables.

4. Run the SQL queries in `basicQueries.sql` to analyze the data.

5. Implement the `advQueries.sql` for advanced features (views, triggers, procedures).
